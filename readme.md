是基于vs2015 -opencv4.2.0开发的，把代码文件拷进去即可。



2020年1月30日 19:09:44

完成了knn前景提取以及kd树查询

2020年1月31日 04:00:42

完成了前景提取的功能

to do:

将前面的前处理写成一个函数（坐标转换等）

将后处理写成一个函数(坐标的转换等等)

转换之后的坐标根据人头进行滤除，根据ROI进行滤除，坐标转换到物体坐标系

再对滤除剩下的物体进行kalman跟踪，跟踪超过一定帧数，则认定为滞留。判定完成之后，将坐标转换回来，并在主坐标系下画出物体的检测框及滞留红色框。



2020年2月17日00:49:31

将处理函数，封装成了类，简化了main函数

其实没有必要进行kalman跟踪，因为是检测物体滞留，并不是检测流量。

to do:

将前面的前处理写成一个函数（坐标转换等）

将后处理写成一个函数(坐标的转换等等)

转换之后的坐标根据人头进行滤除，根据ROI进行滤除，坐标转换到物体坐标系

再对滤除剩下的物体进行相邻帧IOU匹配，跟踪超过一定帧数，则认定为滞留。判定完成之后，将坐标转换回来，并在主坐标系下画出物体的检测框及滞留红色框。



2020年2月25日 18:51:33
将前面的前处理写成一个函数（坐标转换等）√

将后处理写成一个函数(坐标的转换等等)√

转换之后的坐标根据人头进行滤除，根据ROI进行滤除，坐标转换到物体坐标系√

再对滤除剩下的物体进行相邻帧IOU匹配，跟踪超过一定帧数，则认定为滞留。判定完成之后，将坐标转换回来，并在主坐标系下画出物体的检测框及滞留红色框。√